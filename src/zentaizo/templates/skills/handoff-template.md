---
created: "YYYY-MM-DDTHH:MM:SSZ"
edited_by:
---

<!--
  Scaffolded by `zentaizo next-handoff`: it fills `created`, stamps the first
  `edited_by:` entry, and (when tied to a plan) the spec line below. This whole
  file is handed to the implementing agent by reference, so everything under the
  frontmatter IS the prompt — keep it self-contained, cut surrounding narration,
  and delete this comment once written. Re-run `zentaizo edited <this file>`
  after each revision so the ledger records who wrote it.
-->

Implement the plan at `<spec>` as the authoritative spec.

<Write the self-contained prompt here — name only what this slice needs (scope &
guardrails, branch + base + HEAD, inputs/preflight, verification). See
`skills/plan-and-implement.md` § Handing off for the full authoring checklist.>
